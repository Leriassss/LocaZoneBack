module.exports = (req, res, next) => {
    console.log(req.params)/*
    const douche = [] 
    req.body.douche.forEach(element => {
        douche.push(element.photo)
      });
      req.body = douche[0]
      console.log(req.body)
  // Si la validation réussit, passer au middleware suivant
  next();*/
};
